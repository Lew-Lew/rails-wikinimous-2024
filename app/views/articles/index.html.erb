
<% @articles.each do |article| %>
  <article class="w-50 m-auto mb-3">
    <h2 class="text-primary ">
      <%= link_to article.title, article_path(article.id), class: "text-decoration-none" %>
    </h2>
    <p  class="text-truncate"><%= article.content %></p>
  </article>
<% end %>
